# 后端重设计与全量执行任务单

## 1. 目录结构与包重构
- [ ] 重构 `data/` 目录：创建 `Operators`, `Enemies`, `raw_data`, `parsed_data`，移除废弃的旧目录。
- [ ] 调整 `backend/` 结构：
  - 维持 `backend/src/crawler` 和 `backend/src/parser`。
  - 建立 `backend/function/logic` 和 `backend/function/battle` 包。
  - 建立 `backend/main/` 包及启动脚本。
- [ ] 删除原有的 `backend/src/calculator` 及旧入口文件。

## 2. 爬取与解析 (Data Ingestion)
- [ ] 编写 6星干员名单抓取脚本。
- [ ] 将爬取逻辑调整为：原始网页写入 `data/raw_data`，LLM 清洗后写入 `data/parsed_data`（含特性、天赋拆分）。
- [ ] 执行批量爬取，等待所有6星干员信息下载并清洗完毕。

## 3. 面向对象的干员逻辑动态生成 (Object Logic)
- [ ] 遍历 `data/parsed_data` 中的干员信息。
- [ ] 根据读取到的职业，动态生成或复用 `function/logic/professions.py` 中的基类（如重射手）。
- [ ] 生成对应的干员子类脚本并放入 `function/logic/operators/` 目录下。

## 4. 战斗环境搭建 (Battle Environment)
- [ ] 在 `function/battle` 编写宏观的公式计算器，以实现多干员与敌人的交互环境。
- [ ] 建立敌人实体模型。

## 5. 后端 API 搭建
- [ ] 引入 `FastAPI`，在 `main/` 下搭建 Web API 提供计算服务。
