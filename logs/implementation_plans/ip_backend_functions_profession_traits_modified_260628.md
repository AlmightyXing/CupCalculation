# 子职业特性实现与全文件代码统一计划

根据您的 Review 意见，我已完全明确了需求：
1. **抛弃无用字段**：不需要推断 `block_count`，因为在纯单体出伤计算中它并不重要。
2. **核对子职业数量**：游戏内确实有 71 个子职业，但由于我们目前只爬取了 129 位六星干员的数据，他们恰好只覆盖了其中的 45 个子职业（这是正常现象，有些子职业目前只有五星或四星干员）。我们本次将专注于这 45 个涉及六星干员的子职业。
3. **实现核心输出特性**：对于影响出伤的子职业特性（如剑豪的普攻两连击、投掷手的余震、教官的非阻挡增伤、解放者和阵法术师的常态不攻击等），必须在 `professions.py` 的父类代码中落实对应的逻辑。
4. **统一干员代码**：由于之前的 129 位干员代码是由大模型各自为战生成的，很多干员（比如艾丽妮）在重写 `calculate_normal_hit` 时直接抛弃了“剑豪”父类的两连击设定。为了解决大模型跨文件串联能力弱的问题，我将通过构建专门的重构脚本来强制统一它们。

## Proposed Changes

### 1. 手动精确重写基类模块
#### [MODIFY] `backend/function/logic/professions.py`
我将**直接手动编写** `professions.py`。
- 定义全部 45 个实际出现的子职业类。
- 为影响出伤的子职业（剑豪、投掷手、猎手、解放者等）直接覆写 `calculate_normal_hit` 甚至 `calculate_dps` 机制。例如，解放者在 `calculate_normal_hit` 时返回 0，剑豪返回 `2 * hit`。
- 正确设定每个子职业的 `attack_interval`。

### 2. 构建大模型重构 Agent 脚本
#### [NEW] `backend/src/crawler/batch_refactor.py`
为了防止我手动一个个修改 129 个文件导致错漏，我将编写一个新的全自动化重构脚本 `batch_refactor.py`。
- 脚本会读取每个干员当前的 `.py` 文件，以及最新编写的 `professions.py` 中对应的父类源码。
- 脚本将两者拼接为 Prompt 发送给 Gemini 大模型。
- **核心 Prompt 指令**：要求大模型检查干员文件是否违背或遗漏了父类的输出特性（比如：艾丽妮的普攻必须调用或乘以剑豪的 2 连击；如果覆写了父类方法，必须确保父类的特性系数被融入其中，或者尽量改用 `super()` 继承）。
- 脚本将并发或逐个覆盖重写不符合规范的干员代码，彻底完成文件间的统一。

## Verification Plan
1. 完成 `professions.py` 的编写，并使用几名干员的数据（如艾丽妮、玛恩纳）进行局部测试。
2. 运行 `batch_refactor.py`，记录重构日志，确保 129 位干员与最新的父类设定对齐。

> 请确认是否同意该重构方案？同意后我将立即开始执行。
